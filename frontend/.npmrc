# NPM Registry 配置
# 此文件会使得所有 npm install 命令使用指定的 registry

# 默认使用淘宝镜像（适用于中国大陆）
# 如需使用官方 npm registry，请取消下一行的注释并注释掉淘宝镜像行
registry=https://registry.npmjs.org/
# registry=https://registry.npmmirror.com/

# 如果使用私有 registry 需要认证，可以添加以下配置（请取消注释并填写实际值）
# @your-scope:registry=https://your-private-registry.com
# //your-private-registry.com/:_authToken=${NPM_TOKEN}

# 其他常用 registry 选项：
# 官方 npm: https://registry.npmjs.org/
# 淘宝镜像（旧）: https://registry.npm.taobao.org/ (已废弃)
# 淘宝镜像（新）: https://registry.npmmirror.com/